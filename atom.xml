<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>Huiting_Yang</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://troyeeee673.github.io/</id>
  <link href="https://troyeeee673.github.io/" rel="alternate"/>
  <link href="https://troyeeee673.github.io/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, Huiting_Yang</rights>
  <title>Linux系统编程</title>
  <updated>2026-06-07T08:31:01.343Z</updated>
  <entry>
    <author>
      <name>Huiting_Yang</name>
    </author>
    <category term="Linux系统编程" scheme="https://troyeeee673.github.io/categories/Linux%E7%B3%BB%E7%BB%9F%E7%BC%96%E7%A8%8B/"/>
    <category term="软件相关" scheme="https://troyeeee673.github.io/tags/%E8%BD%AF%E4%BB%B6%E7%9B%B8%E5%85%B3/"/>
    <category term="Linux系统编程" scheme="https://troyeeee673.github.io/tags/Linux%E7%B3%BB%E7%BB%9F%E7%BC%96%E7%A8%8B/"/>
    <category term="C语言" scheme="https://troyeeee673.github.io/tags/C%E8%AF%AD%E8%A8%80/"/>
    <content>
      <![CDATA[<h1 id="系统编程：I-O-基础与标准-I-O-实战笔记"><a href="#系统编程：I-O-基础与标准-I-O-实战笔记" class="headerlink" title="系统编程：I&#x2F;O 基础与标准 I&#x2F;O 实战笔记"></a>系统编程：I&#x2F;O 基础与标准 I&#x2F;O 实战笔记</h1><h2 id="一、-I-O的重要性"><a href="#一、-I-O的重要性" class="headerlink" title="一、 I&#x2F;O的重要性"></a>一、 I&#x2F;O的重要性</h2><p>在 Linux&#x2F;Unix 系统编程中，I&#x2F;O 是最基础也最容易被低估的一部分。无论是普通文件、目录、设备、管道还是 socket，最终都会以“可读写对象”的形式参与程序运行。先把标准 I&#x2F;O 和文件对象理解清楚，后面学习系统调用 I&#x2F;O、进程间通信、网络通信时会顺很多。在Linux中有一句很经典的话：“一切皆文件”，所有我们在linux中进行的操作都是基于文件进行的，而对文件操作无非就是读写，涉及到读写就必须借助IO。所以说IO很重要。</p><p>重点可以概括为一句话：区分标准 I&#x2F;O 与系统调用 I&#x2F;O，掌握文件打开模式、缓冲机制、文件定位、目录操作和常见练习任务。</p><h2 id="二、文件概念：狭义文件与广义文件"><a href="#二、文件概念：狭义文件与广义文件" class="headerlink" title="二、文件概念：狭义文件与广义文件"></a>二、文件概念：狭义文件与广义文件</h2><p>文件的分类：</p><ul><li>狭义：文件和文件夹。</li><li>广义：普通文件 <code>-</code>、目录 <code>d</code>、块设备 <code>b</code>、字符设备 <code>c</code>、链接 <code>l</code>、管道 <code>p</code>、套接字 <code>s</code>。</li></ul><p>这也是“一切皆文件”思想的入口。系统编程里说的文件，往往不是只指磁盘上的 <code>.txt</code>、<code>.c</code>，而是指内核暴露出来的一类统一接口。比如终端可以读写，管道可以读写，网络 socket 也可以读写。理解这一点后，<code>read/write</code>、<code>select/poll/epoll</code> 等机制才会显得自然。</p><h2 id="三、标准-I-O：围绕-FILE-展开"><a href="#三、标准-I-O：围绕-FILE-展开" class="headerlink" title="三、标准 I&#x2F;O：围绕 FILE * 展开"></a>三、标准 I&#x2F;O：围绕 <code>FILE *</code> 展开</h2><p>标准 I&#x2F;O 来自 C 标准库，核心对象是 <code>FILE *</code>。<strong>“<code>FILE *</code> 贯穿始终”</strong>。调用 <code>fopen()</code> 成功后得到的不是内核文件描述符，而是 C 库维护的文件流对象。</p><p>常见函数包括：</p><ul><li><code>fopen()</code>：打开文件。</li><li><code>fclose()</code>：关闭文件，并刷新缓冲区。</li><li><code>fread()</code> &#x2F; <code>fwrite()</code>：按二进制块读写。</li><li><code>fgetc()</code> &#x2F; <code>fputc()</code>：按字符读写。</li><li><code>fgets()</code> &#x2F; <code>fputs()</code>：按字符串读写。</li><li><code>fseek()</code> &#x2F; <code>ftell()</code> &#x2F; <code>rewind()</code>：文件定位。</li><li><code>fprintf()</code>、<code>perror()</code>、<code>strerror()</code>：格式化输出与错误处理。</li></ul><p>错误处理时要注意：<code>errno</code> 是宏，对应各种错误原因。实际写程序时，系统调用或库函数失败后，优先用 <code>perror(&quot;xxx&quot;)</code> 输出上下文，调试效率更高。</p><h2 id="四、文件打开模式"><a href="#四、文件打开模式" class="headerlink" title="四、文件打开模式"></a>四、文件打开模式</h2><p>常用 <code>mode</code>：</p><ul><li><code>r</code>：只读，要求文件必须存在。</li><li><code>r+</code>：读写，要求文件必须存在。</li><li><code>w</code>：只写，有则清空，无则创建。</li><li><code>w+</code>：读写，有则清空，无则创建。</li><li><code>a</code>：追加写，无则创建。</li><li><code>a+</code>：读和追加写，无则创建。</li></ul><p>这里最容易踩坑的是 <code>w</code> 和 <code>w+</code>：只要打开成功，原文件内容就会被截断。如果程序本意是“在文件末尾继续写”，应该使用 <code>a</code> 或 <code>a+</code>。<br>如果使用’r’或’r+’打开一个不存在的文件，<code>fopen()</code>会返回 <code>NULL</code>，并设置 <code>errno</code> 为 <code>ENOENT</code>（No such file or directory）。因此在使用这些模式时，务必检查返回值，避免后续操作在空指针上崩溃。</p><p>不过从工程角度看，逐字符复制效率较低。更推荐用 <code>fread()</code> &#x2F; <code>fwrite()</code> 按块复制：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span> ((n = fread(buf, <span class="number">1</span>, BUFFERSIZE, fds)) &gt; <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    fwrite(buf, <span class="number">1</span>, n, fde);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>fread(buf, size, nmemb, fp)</code> 的返回值是成功读到的对象个数。为了让返回值直接表示字节数，常见写法是把 <code>size</code> 设为 <code>1</code>，<code>nmemb</code> 设为缓冲区大小。</p><h2 id="六、fgets-与-gets-：安全边界"><a href="#六、fgets-与-gets-：安全边界" class="headerlink" title="六、fgets() 与 gets()：安全边界"></a>六、<code>fgets()</code> 与 <code>gets()</code>：安全边界</h2><p>提醒一个非常重要的问题：<code>gets()</code> 不检查缓冲区大小，因此不推荐使用。现代编程中应始终使用 <code>fgets()</code>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> *<span class="title function_">fgets</span><span class="params">(<span class="type">char</span> *s, <span class="type">int</span> size, FILE *stream)</span>;</span><br></pre></td></tr></table></figure><p><code>fgets()</code> 的结束条件是：读到换行符、缓冲区满、或遇到文件结束。它会保留换行符，这一点在做字符串比较或命令解析时要特别处理。</p><p>如果要读取任意长度的一整行，GNU 扩展提供了 <code>getline()</code>(这也是唯一一个完全意义上的能够完整读取一行的函数)：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> _GNU_SOURCE</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">ssize_t</span> <span class="title function_">getline</span><span class="params">(<span class="type">char</span> **lineptr, <span class="type">size_t</span> *n, FILE *stream)</span>;</span><br></pre></td></tr></table></figure><p>注意：<code>getline()</code> 本质上会在空间不足时自动 <code>malloc/realloc</code>，因此使用完需要 <code>free(lineptr)</code>。</p><h2 id="七、文件定位：fseek-、ftell-、rewind"><a href="#七、文件定位：fseek-、ftell-、rewind" class="headerlink" title="七、文件定位：fseek()、ftell()、rewind()"></a>七、文件定位：<code>fseek()</code>、<code>ftell()</code>、<code>rewind()</code></h2><p>常用接口如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">fseek</span><span class="params">(FILE* stream, <span class="type">long</span> offset, <span class="type">int</span> whence)</span>;</span><br><span class="line"><span class="type">long</span> <span class="title function_">ftell</span><span class="params">(FILE* stream)</span>;</span><br><span class="line"><span class="type">void</span> <span class="title function_">rewind</span><span class="params">(FILE* stream)</span>;</span><br></pre></td></tr></table></figure><p><code>whence</code> 可以取：</p><ul><li><code>SEEK_SET</code>：从文件开头开始偏移。</li><li><code>SEEK_CUR</code>：从当前位置开始偏移。</li><li><code>SEEK_END</code>：从文件末尾开始偏移。</li></ul><p>这里就不得不提及一下空洞文件：当 <code>fseek()</code> 把文件位置移动到文件末尾之后更远的位置，再写入数据，中间跳过的部分会表现为全零字节，但通常不立即占用实际磁盘块。这是文件系统层面的优化，常用于提前占位或构造稀疏文件。</p><p>查看文件大小的例子：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">FILE *fp = fopen(argv[<span class="number">1</span>], <span class="string">&quot;r&quot;</span>);</span><br><span class="line">fseek(fp, <span class="number">0</span>, SEEK_END);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%ld&quot;</span>, ftell(fp));</span><br><span class="line">fclose(fp);</span><br></pre></td></tr></table></figure><p>需要注意：<code>fseek()</code> &#x2F; <code>ftell()</code> 的 <code>long</code> 在某些平台上可能不足以处理大文件。POSIX 提供了 <code>fseeko()</code> &#x2F; <code>ftello()</code>，配合 <code>_FILE_OFFSET_BITS=64</code> 更适合大文件场景。</p><h2 id="八、缓冲机制：标准-I-O-为什么快"><a href="#八、缓冲机制：标准-I-O-为什么快" class="headerlink" title="八、缓冲机制：标准 I&#x2F;O 为什么快"></a>八、缓冲机制：标准 I&#x2F;O 为什么快</h2><h3 id="1-标准-I-O-与系统-I-O-的区别"><a href="#1-标准-I-O-与系统-I-O-的区别" class="headerlink" title="1. 标准 I&#x2F;O 与系统 I&#x2F;O 的区别"></a>1. 标准 I&#x2F;O 与系统 I&#x2F;O 的区别</h3><p>Linux 中的 I&#x2F;O 可以分为两层：</p><ul><li><p><strong>系统 I&#x2F;O（System Call I&#x2F;O）</strong></p><ul><li>open()</li><li>read()</li><li>write()</li><li>close()</li></ul></li><li><p><strong>标准 I&#x2F;O（stdio）</strong></p><ul><li>fopen()</li><li>fread()</li><li>fwrite()</li><li>fprintf()</li><li>fclose()</li></ul></li></ul><p>标准 I&#x2F;O 本质上是对系统 I&#x2F;O 的封装，其底层最终仍然会调用 <code>read()</code> 和 <code>write()</code>。</p><h3 id="2-为什么标准-I-O-更快"><a href="#2-为什么标准-I-O-更快" class="headerlink" title="2. 为什么标准 I&#x2F;O 更快"></a>2. 为什么标准 I&#x2F;O 更快</h3><p>这里的“快”并不是指单次响应速度快，而是指：</p><blockquote><p><strong>单位时间内处理的数据更多（吞吐量更高）。</strong></p></blockquote><p>原因是：</p><ul><li>(1) <strong>系统调用代价很高</strong></li></ul><p>每调用一次 <code>read()</code> 或 <code>write()</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">用户态</span><br><span class="line">   ↓</span><br><span class="line">内核态</span><br><span class="line">   ↓</span><br><span class="line">执行I/O</span><br><span class="line">   ↓</span><br><span class="line">返回用户态</span><br></pre></td></tr></table></figure><p>都需要进行：</p><ul><li>CPU 特权级切换</li><li>寄存器保存恢复</li><li>内核调度检查</li></ul><p>这些操作都会消耗时间。</p><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">1000000</span>; i++)</span><br><span class="line">&#123;</span><br><span class="line">    write(fd, &amp;ch, <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>会产生：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">1000000 次 write()</span><br><span class="line"></span><br><span class="line">1000000 次用户态-&gt;内核态切换</span><br></pre></td></tr></table></figure><p>效率极低。</p><ul><li>(2) <strong>标准 I&#x2F;O 使用缓冲区减少系统调用次数</strong></li></ul><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">FILE *fp = fopen(<span class="string">&quot;test.txt&quot;</span>,<span class="string">&quot;w&quot;</span>);</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;<span class="number">1000000</span>;i++)</span><br><span class="line">&#123;</span><br><span class="line">    fputc(ch,fp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>并不会执行：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">1000000 次 write()</span><br></pre></td></tr></table></figure><p>而是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">先写入用户空间缓冲区</span><br><span class="line">缓冲区满后一次 write()</span><br></pre></td></tr></table></figure><p>例如缓冲区大小：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">4096 Bytes</span><br></pre></td></tr></table></figure><p>那么：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">1000000 字节数据</span><br><span class="line">系统IO：</span><br><span class="line">1000000 次 write()</span><br><span class="line">标准IO：</span><br><span class="line">1000000 / 4096 ≈ 245 次 write()</span><br></pre></td></tr></table></figure><p>系统调用次数减少了约：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">1000000 → 245</span><br></pre></td></tr></table></figure><p>因此吞吐量大幅提高。</p><h3 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h3><p>假设复制一个 1GB 文件：</p><h4 id="方法一：每次读写-1-字节"><a href="#方法一：每次读写-1-字节" class="headerlink" title="方法一：每次读写 1 字节"></a>方法一：每次读写 1 字节</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span>((ch = fgetc(fp1)) != EOF)</span><br><span class="line">&#123;</span><br><span class="line">    fputc(ch, fp2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>底层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">大量系统调用</span><br></pre></td></tr></table></figure><p>效率较低。</p><hr><h4 id="方法二：块读写"><a href="#方法二：块读写" class="headerlink" title="方法二：块读写"></a>方法二：块读写</h4><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> buf[<span class="number">4096</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span>((n = fread(buf,<span class="number">1</span>,<span class="keyword">sizeof</span>(buf),fp1)) &gt; <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    fwrite(buf,<span class="number">1</span>,n,fp2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>底层：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">每次处理4096字节</span><br></pre></td></tr></table></figure><p>效率明显提高。</p><hr><h3 id="3-三种缓冲方式"><a href="#3-三种缓冲方式" class="headerlink" title="3. 三种缓冲方式"></a>3. 三种缓冲方式</h3><h4 id="行缓冲（Line-Buffer）"><a href="#行缓冲（Line-Buffer）" class="headerlink" title="行缓冲（Line Buffer）"></a>行缓冲（Line Buffer）</h4><p>遇到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">换行符 &#x27;\n&#x27;</span><br></pre></td></tr></table></figure><p>立即刷新。</p><p>常见：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">stdout（连接终端时）</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;hello\n&quot;</span>);</span><br></pre></td></tr></table></figure><p>立即显示。</p><hr><h4 id="全缓冲（Full-Buffer）"><a href="#全缓冲（Full-Buffer）" class="headerlink" title="全缓冲（Full Buffer）"></a>全缓冲（Full Buffer）</h4><p>满足以下条件之一才刷新：</p><ul><li>缓冲区满</li><li>fflush()</li><li>fclose()</li></ul><p>常见：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">普通文件</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">fprintf</span>(fp,<span class="string">&quot;hello&quot;</span>);</span><br></pre></td></tr></table></figure><p>可能不会立刻写入磁盘。</p><hr><h4 id="无缓冲（Unbuffered）"><a href="#无缓冲（Unbuffered）" class="headerlink" title="无缓冲（Unbuffered）"></a>无缓冲（Unbuffered）</h4><p>不经过缓冲区。</p><p>典型：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">stderr</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">fprintf</span>(<span class="built_in">stderr</span>,<span class="string">&quot;error\n&quot;</span>);</span><br></pre></td></tr></table></figure><p>立即输出。</p><hr><h3 id="4-fflush"><a href="#4-fflush" class="headerlink" title="4. fflush()"></a>4. fflush()</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">fflush</span><span class="params">(FILE *stream)</span>;</span><br></pre></td></tr></table></figure><p>作用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">强制刷新缓冲区</span><br></pre></td></tr></table></figure><p>如果：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fflush(<span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure><p>则刷新：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">所有打开的输出流</span><br></pre></td></tr></table></figure><hr><h3 id="5-为什么下面代码看不到输出"><a href="#5-为什么下面代码看不到输出" class="headerlink" title="5. 为什么下面代码看不到输出"></a>5. 为什么下面代码看不到输出</h3><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;before while&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span>(<span class="number">1</span>);</span><br></pre></td></tr></table></figure><p>原因：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">stdout连接终端时采用行缓冲</span><br></pre></td></tr></table></figure><p>而：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;before while&quot;</span>);</span><br></pre></td></tr></table></figure><p>没有：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">换行符 &#x27;\n&#x27;</span><br></pre></td></tr></table></figure><p>因此：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">数据仍停留在缓冲区</span><br></pre></td></tr></table></figure><p>程序进入死循环后无法刷新缓冲区，所以终端看不到输出。</p><p>解决方法：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;before while\n&quot;</span>);</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;before while&quot;</span>);</span><br><span class="line"></span><br><span class="line">fflush(<span class="built_in">stdout</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span>(<span class="number">1</span>);</span><br></pre></td></tr></table></figure><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul><li><strong>系统 I&#x2F;O 响应更快（调用立即执行）</strong></li><li><strong>标准 I&#x2F;O 吞吐量更高（缓冲减少系统调用）</strong></li><li><strong>标准 I&#x2F;O 底层仍然依赖系统 I&#x2F;O</strong></li><li><strong>两者都能完成任务时，优先使用标准 I&#x2F;O</strong></li></ul><h2 id="九、临时文件与目录操作"><a href="#九、临时文件与目录操作" class="headerlink" title="九、临时文件与目录操作"></a>九、临时文件与目录操作</h2><p>临时文件要解决两个问题：命名冲突和及时销毁。<br>C 标准库提供了两个接口：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> *<span class="title function_">tmpnam</span><span class="params">(<span class="type">char</span> *s)</span>;</span><br><span class="line">FILE *<span class="title function_">tmpfile</span><span class="params">(<span class="type">void</span>)</span>;</span><br></pre></td></tr></table></figure><p>实践中更推荐 <code>tmpfile()</code> 或平台提供的安全临时文件创建函数，因为 <code>tmpnam()</code> 存在竞态风险。<br>tmpnam() 竞态风险分析:在多个进程或线程同时调用 tmpnam() 时，可能会生成相同的临时文件名，导致命名冲突和安全问题。</p><p>目录操作相关 API：</p><ul><li><code>opendir()</code></li><li><code>closedir()</code></li><li><code>readdir()</code></li><li><code>rewinddir()</code></li><li><code>seekdir()</code></li><li><code>telldir()</code></li></ul><p>目录遍历是后续实现 <code>ls</code>、统计目录文件数量、递归统计源码行数等练习的基础。</p><h2 id="十、练习任务"><a href="#十、练习任务" class="headerlink" title="十、练习任务"></a>十、练习任务</h2><p>巩固任务：</p><ul><li>（1）手动实现一个cp命令。(这段代码有一个很重要的细节：如果目标文件打开失败，要先关闭已经打开的源文件。系统编程中资源释放顺序非常重要，尤其是后续涉及文件锁、进程、线程时。)</li><li>（2）手动实现一个 <code>getline()</code>，理解动态扩容。</li><li>（3）手动实现 <code>wc</code> 命令，统计行数、单词数、字节数。</li><li>（4）统计 C 源文件总行数、空行数、注释行数和代码行数。</li><li>（5）使用 <code>fseek()</code> &#x2F; <code>ftell()</code> 切分文件。</li><li>（6）基于 <code>DIR</code> 实现一个简化版 <code>ls</code>。</li><li>（7）统计Linux内核源码目录中文件数、目录数以及所有 <code>.c/.h</code> 文件总行数。</li></ul><p><code>练习任务代码开源地址</code>:<a href="https://github.com/troyeeee673/Linux-">https://github.com/troyeeee673/Linux-</a></p><h2 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h2><p>标准 I&#x2F;O 的价值在于简单、可移植、吞吐量高。它通过缓冲减少系统调用次数，适合大多数普通文件读写场景。真正需要精细控制文件描述符、阻塞行为、文件锁、I&#x2F;O 复用时，再进入系统调用 I&#x2F;O。学完这一章，最重要的是把 <code>FILE *</code>、缓冲、文件定位和资源释放这几个点练熟。</p>]]>
    </content>
    <id>https://troyeeee673.github.io/2026/06/07/%E7%AC%AC%E4%B8%80%E7%AB%A0%E3%80%81%E6%A0%87%E5%87%86IO/</id>
    <link href="https://troyeeee673.github.io/2026/06/07/%E7%AC%AC%E4%B8%80%E7%AB%A0%E3%80%81%E6%A0%87%E5%87%86IO/"/>
    <published>2026-06-07T08:30:01.000Z</published>
    <summary>
      <![CDATA[<h1 id="系统编程：I-O-基础与标准-I-O-实战笔记"><a href="#系统编程：I-O-基础与标准-I-O-实战笔记" class="headerlink" title="系统编程：I&#x2F;O 基础与标准 I&#x2F;O 实战笔记"></a>系统编程：I&]]>
    </summary>
    <title>第一章、标准IO</title>
    <updated>2026-06-07T08:31:01.343Z</updated>
  </entry>
</feed>
